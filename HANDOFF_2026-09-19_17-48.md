# Project session handoff

Created: `2026-09-19T17:48:19+09:00`
Project root: `/Users/natatekoko/AI/新開発ファイル/シャドバ戦績研究`

## Resume first

- Read: `プロジェクト開始.md`, `README.md`, and the files directly related to the new request.
- Run if relevant: `git status --short --branch`, `git log -1 --oneline`, then targeted tests or builds for changed code.
- Do not: recreate the project elsewhere, delete the installed iPhone app before an update, or treat old verification logs as proof for current source.

## Current objective

Maintain a personal iPhone app for fast Shadowverse: Worlds Beyond match recording and matchup research. The next session should continue from this repository without relying on the previous chat transcript.

## Confirmed current state

- The folder is an independent Git repository on branch `main` with GitHub remote `nadatecoco/shadowverse-worlds-beyond-records`.
- The latest source has a fixed seven-class matchup matrix and class-level subjective ratings.
- Existing Action Button, App Shortcut, Interactive Snippet, Control, Live Activity, history, deck management, and BO1 code remains in the repository.
- Xcode 26.5 is installed. The deployment target remains iOS 26.0.
- Previous records show a signed Build 11 installation on an iPhone Air. That does not prove that commit `960bd6f` is installed.

## Completed in this session

- Confirmed the canonical project folder and Git state.
- Added `プロジェクト開始.md` as the human and Codex entry point, including implementation lessons, Apple API boundaries, wireless device conditions, and resume steps.
- Added this handoff for the next session.

## Incomplete work

- The local project is not yet registered in Codex's upper Projects section; the available Codex project tools can list projects but cannot create a saved local project entry.
- Commit `960bd6f` has not been proven by a clean full Xcode test/build after the earlier CoreSimulator service failure.
- The latest matchup-table source has not been installed and visually checked on the iPhone.
- Physical Action Button behavior and game-over-Snippet behavior remain device checks.

## Decisions and reasons

- Use the existing `シャドバ戦績研究` folder as the project root instead of copying it. It already contains the source, Git history, documentation, tests, and evidence.
- Keep a single root handoff and a separate project-start document. The handoff records transient state; `プロジェクト開始.md` records stable operating knowledge.
- Wireless Xcode deployment is treated as available only after Mac–iPhone pairing and device discovery, not as automatic installation merely because the phone is unlocked nearby.

## Risks and possibly broken areas

- `検証結果/検証結果.md` contains historical results from several builds. Read dates and build numbers; do not assume every line describes current source.
- The last full Xcode attempt was blocked by a local CoreSimulator/simdiskimaged failure. Swift parsing and a domain smoke test passed, but that is not a replacement for the full build.
- iOS controls and snippets are partly system-managed and need physical-device confirmation.

## Verification

- `git status --short --branch`: pass; `main...origin/main`, clean before these new documentation files.
- `git log -1 --oneline`: `960bd6f 相性表をクラス固定マトリクスへ再構築`.
- `xcodebuild -version`: Xcode 26.5, Build 17F42.
- Current device discovery: not confirmed in this session.

## Git snapshot

- Branch: `main`
- HEAD: `960bd6f 相性表をクラス固定マトリクスへ再構築`
- Worktree: this handoff and `プロジェクト開始.md` are the only session changes; the completed handoff commit should leave it clean.
- Remote/publication state: HEAD `960bd6f` is already pushed to `origin/main`.

## Next smallest step

Register `/Users/natatekoko/AI/新開発ファイル/シャドバ戦績研究` as a Codex local project. Then restore the Xcode simulator/device service, run the unit and UI tests, build the current source, and update the paired iPhone without deleting its data.

## Key files

- `プロジェクト開始.md`: stable project entry point and implementation rules.
- `Sources/Domain.swift`: version 3 data model and class matchup calculations.
- `Sources/AnalysisViews.swift`: seven-class matrix and detail editor.
- `Sources/Intents.swift`: App Shortcut and Snippet entry.
- `Sources/Store.swift`: persistence and migrations.
- `Tests/DomainTests.swift`: domain and migration coverage.

## Notes for historical interpretation

The chat contained several successive fast-input designs. Current source files and the newest explicit user request outrank older prose in historical logs. The repository commit time does not by itself prove device installation or physical-button behavior.
