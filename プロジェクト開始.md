# シャドバ戦績研究：プロジェクト開始

このフォルダが「シャドバ戦績研究」iPhoneアプリの正式な作業場所です。次のセッションでは、このファイルとルート直下の最新`HANDOFF_*.md`を最初に読んでください。

## 場所とリポジトリ

- Finderで見える場所：`AI/新開発ファイル/シャドバ戦績研究`
- Codexの実作業場所：`/Users/natatekoko/AI/新開発ファイル/シャドバ戦績研究`
- Xcodeで開くファイル：`シャドバ戦績研究.xcodeproj`
- 実行スキーム：`ShadowRecord`
- GitHub：<https://github.com/nadatecoco/shadowverse-worlds-beyond-records>

コピーやリンクではなく、このフォルダ自体がGitリポジトリです。別の作業用コピーを作らず、ここを正本として扱います。

## アプリの目的

1. Shadowverse: Worlds Beyondの対戦結果を少ない操作で記録する。
2. 自分の主観相性と実戦値を並べ、相性表と2デッキBO1の選出を研究する。

データは端末内のSwiftDataへ保存します。アカウント、サーバー、外部ライブラリは使いません。

## 現在の主な機能

- 戦績の追加・編集・削除、日時・勝敗・先後・相手クラス・アーキタイプ・メモの保存
- 7クラス固定の相性表。クラス単位の🧠主観値と📊実戦値を分けて表示
- クラス詳細からアーキタイプ別の内訳と従来の主観値を確認・編集
- 現在環境、任意環境、全期間の切り替え
- 2デッキBO1の2×2比較、最低値、クラス分布による期待相性
- デッキ、アーキタイプ、環境、クラス絵文字の管理
- JSON書き出し・復元
- App Shortcut、Interactive Snippet、Control Center用Control、Live Activity

## 実装上の重要な考え方

### 高速入力

- Action Buttonの主経路は、アプリ内蔵の「戦績を記録」App ShortcutからInteractive Snippetを呼び出す構成です。
- ユーザーがShortcutsアプリでショートカットを自作する必要はありません。
- 本体アプリ内の「高速入力を試す」は、同じ保存処理を確認するアプリ内画面です。通常アプリ内からシステムSnippetを強制表示する公開APIはありません。
- Control CenterのControlはSnippetを表示せず、本体の高速入力画面を開きます。
- Action Button設定画面へ直接移動する公開APIはありません。アプリ固有の設定画面をAction Button設定への入口として使わないでください。

### 保存

- 小規模な個人用データとして、`AppData`全体をSwiftDataの`LocalArchive`一件へ保存します。
- 本体とApp Intentは同じ`Store`を通じて保存し、別データベースへ二重管理しません。
- 現在のJSON形式はversion 3です。version 1・2を読み込み、既存戦績を残して移行します。
- 高速入力用の標準7クラスには固定UUIDがあります。通常のデッキ一覧や相性表では、標準候補とユーザー登録デッキを混同しないようにします。

### 相性表

- 第一画面は常に「自分の登録デッキ × 相手7クラス」です。相手アーキタイプが0件でも表示します。
- クラス主観値は`ClassOpinion`として、環境・自分デッキ・相手クラスごとに独立保存します。
- クラス実戦値は`Match.opponentClass`で集計し、相手アーキタイプ不明（`opponentID == nil`）の試合も含めます。
- アーキタイプ主観値は従来の`Opinion`として残し、クラスセルの詳細に表示します。
- BO1で相手クラスしか分からない場合の計算は、アーキタイプ分布を使う従来ロジックです。クラス相性表の単純集計とは用途を分けています。

### Apple側の制約

- Interactive Snippet、Dynamic Island、Live Activityの位置・閉じ方・表示期間はiOSが管理します。
- 他社ゲーム上の任意位置へ常設する独自overlay、PiPの転用、非公開URL Schemeは使いません。
- 右側のカメラコントロールは、通常のAction Buttonのように任意のApp Shortcutへ置き換えるボタンではありません。
- シミュレータ成功、実機へのインストール成功、物理Action Buttonでの成功は別々に記録してください。

## iPhoneへ無線で入れる条件

毎回USB接続する必要はありません。一度MacとiPhoneを信頼・ペアリングし、iPhoneの開発者モードを有効にした後は、XcodeのDevice Hubでペアリング済み端末が見えていれば無線でビルド・インストールできます。MacとiPhoneは通信できる同一ネットワークに置き、iPhoneをロック解除しておくと認識が安定します。

無線で端末が出ない場合は、一度USBで接続して信頼状態とペアリングを確認します。アプリの開発署名期限、端末登録、XcodeのApple Account設定はUSBの有無とは別に必要です。

Apple公式：[実機またはシミュレータでアプリを実行する](https://developer.apple.com/documentation/xcode/running-your-app-on-simulated-or-physical-devices)／[Macと実機をペアリングする](https://developer.apple.com/documentation/xcode/pairing-your-devices-with-your-mac)

## 次のセッションの開始手順

1. このフォルダをCodexのローカルプロジェクトとして開く。
2. このファイルと最新の`HANDOFF_*.md`を読む。
3. `git status --short --branch`と`git log -1 --oneline`を確認する。
4. 実装を変更する場合は、対象コードと現在のテスト結果を確認してから作業する。
5. 実機更新を頼まれた場合は、最新ソースの署名付きビルド成功後に上書きインストールする。既存アプリを削除しない。

## 詳細資料

- `README.md`：機能と技術上の制約
- `使い方.md`：ユーザー向け操作とデータ管理
- `検証結果/検証結果.md`：過去のビルド、テスト、実機確認の記録
- `Sources/Domain.swift`：モデル、検証、集計、相性計算
- `Sources/Store.swift`：SwiftData保存、移行、JSON入出力
- `Sources/AnalysisViews.swift`：相性表と持ち込み解析
- `Sources/Intents.swift`、`Sources/OneTapRecords.swift`：App Shortcutと高速入力
