# シャドバ戦績研究

Shadowverse: Worlds Beyondの戦績を記録し、主観相性と実戦値を比較する非公式の個人用iPhoneアプリです。データは端末内だけに保存します。

## 初回の起動

1. 「シャドバ戦績研究.xcodeproj」をXcode 26以降で開きます。
2. `ShadowRecord`ターゲットの「Signing & Capabilities」で自分の開発チームを選びます。必要ならBundle Identifierを自分のものへ変更してください。
3. iOS 26以降のiPhoneを接続し、実行先に選んでRunします。端末側の信頼・開発者モードの設定が求められたら画面に従ってください。
4. これで高速入力を使えます。7クラスと「現在の環境」は自動で用意されます。
5. 必要な場合だけ「デッキ／設定」で自分の候補、相手アーキタイプ、環境を追加・編集します。

署名済みの配布アプリではありません。実機インストールには自分のXcode署名設定が必要です。本体アプリはシミュレータでは開発チームなしで実行できます。システムからApp Shortcutを実行する検証では、開発証明書による署名が必要でした。

## ⚡ 高速入力で戦績を記録

本体の「⚡ 高速入力を試す」をタップすると、実際の入力と保存をアプリ内で確認できます。Action Buttonから起動した場合は、本体へ移動せず、iOSのInteractive Snippetへ同じ項目を表示します。通常アプリ内からシステムのSnippetを強制表示する公開APIはありません。

デッキや環境を先に登録する必要はありません。初回から自分側の7クラスを選べます。

1. iPhoneの設定 → アクションボタン → ショートカット → ショートカットを選択 → **アプリ欄の「シャドバ戦績研究」→「戦績を記録」**を選びます。Shortcutsアプリで自作する必要はありません。
2. iPhone左側、音量ボタンより上の物理Action Buttonを長押しします。右側のカメラコントロールとは別です。
3. 小さい画面で自分の候補と相手クラスを選びます。相手のアーキタイプ登録がある場合は任意で選べます。
4. 「先攻勝ち／後攻勝ち／先攻負け／後攻負け」を選び、「保存」を押します。

保存した試合は、日時、自分の候補、相手クラス、相手アーキタイプ、勝敗、先後を持つ通常の戦績として履歴へすぐ反映されます。同じ入力画面で保存を連打しても、1試合だけ記録します。

「デッキ／設定」→「自分の候補を追加・編集」では、標準7クラスに「エルフ（ダストデイズエルフ）」「エルフ（リノセウスエルフ）」のような候補を追加できます。名前とクラスを指定するだけで、高速入力の自分側候補と相手アーキタイプ候補へ反映されます。

## ライブ戦績

本体上部で「ライブ戦績を開始」を押すと、現在環境・現在使用中デッキの集計を表示します。終了は「ライブ戦績を終了」。勝敗、勝率、デッキ名、連勝数を表示し、狭いDynamic Islandでは勝敗だけに絞ります。戦績の追加・修正・削除・取り消し、使用デッキ・環境の変更で更新します。

Live ActivityはOSが表示位置と期間を管理し、通常最長8時間です。独自overlayではなく、ゲーム左上へ固定することはできません。横画面・ロック状態での見え方は端末とOSによります。表示が終了していても戦績は保存できます。インターネットやプッシュ通知は使いません。

## Control Centerとその他の入口

右上から下へスワイプ → 空いた場所を長押し → コントロールを追加 → このアプリの「戦績入力」。ホームボタン搭載端末では下端から上へスワイプします。本体の高速入力画面が直接開き、相手情報も入力できます。ロック画面・Action Buttonのコントロールにも配置できます。

Action ButtonがないiPhoneはControl Centerを推奨します。背面タップは保存済みショートカットを割り当てる仕組みで、内蔵操作の直接選択は実機未確認です。

## Apple APIと保存の制約

- iOS 26以降。Snippetの表示と閉じる操作、Dynamic Islandの位置や優先度はOSが管理します。ゲームの一時停止や画面復帰はゲーム側の挙動にも依存します。
- Control CenterからはSnippetを表示せず、本体を開きます。
- Action Button専用設定へ飛ぶ公開APIは使用できないため、設定手順を案内します。アクセス許可画面へ送るボタンはありません。
- LiveActivityIntentを本体プロセスで実行して同じSwiftDataへ保存します。Widget側はActivityのContentStateを描画するだけで、DBの二重管理やApp Groupへの移動は行いません。
- 初回に旧形式を移行し、変更前のJSONをDocumentsの「移行前バックアップ」に保存します。移行に失敗した場合は初期化せずエラーを表示します。旧JSONも読み込めます。新版のJSONは旧版アプリへ戻せません。
- ロック中は端末認証が必要になる場合があります。使用デッキが削除・アーカイブされた場合は本体で選び直してください。

参考：[SnippetIntent](https://developer.apple.com/documentation/appintents/snippetintent)、[LiveActivityIntent](https://developer.apple.com/documentation/appintents/liveactivityintent)、[ActivityKit](https://developer.apple.com/documentation/activitykit/displaying-live-data-with-live-activities)

## クラスアイコンの変更

「デッキ／設定」→「クラスアイコンを変更」で、7クラスの絵文字を1つずつ編集して保存できます。標準に戻すボタンも用意しています。本体・高速入力に反映され、JSONバックアップにも含まれます。クラスの分類や過去の戦績は変わりません。画像ファイルへの差し替えには対応していません。

## 📊 戦績・相性表

戦績タブでは全体、クラス、アーキタイプ×先後、先後別の勝率を確認できます。履歴をタップすると、デッキ・環境・相手・日時・勝敗・メモを修正できます。左スワイプから削除できます。

相性表のセルには、手入力の🧠主観値と、戦績から計算する📊実戦値を分けて表示します。セルをタップすると0〜100%の主観値を1%刻みで編集でき、勝敗数と先後別成績も確認できます。主観未設定を50%や0%で埋めることはありません。

件数の目安は、1〜9戦が「🌧️ サンプル少」、10〜29戦が「☁️ 中程度」、30戦以上が「☀️ 十分」です。0戦は「未対戦」です。これは件数を示す便宜的な区分で、精度の統計的保証ではありません。

戦績・相性表・持ち込みの環境選択は共通です。初期値は現在環境。全期間の実戦値だけは全環境を集計します。全期間の主観値とアーキタイプ分布は独立した手入力設定で、環境別の設定を平均・転用しません。

## 🧠 2デッキBO1

自分の異なる2デッキと相手の2候補を選択します。相手は各枠でアーキタイプまたはクラスを指定できます。2×2の各値と、自分の各行の最小値を表示します。最小値が高い側を「安全候補」とします。同値なら同等です。

この「最低値」は選んだ相手2候補に対する入力値の最小値で、実際の勝率の保証ではありません。必要なセルが欠けていれば比較不能とし、主観値で実戦値を補うこともしません。

相手がクラスしか分からない場合は、設定でそのクラスのアーキタイプ分布を作ります。例：ランプ70%・フェイス30%。合計100%で保存し、各相性の加重平均を計算します。正の割合の候補に未設定・未対戦が1つでもあれば期待値を表示しません。

クラス期待値の件数表示は、構成アーキタイプのうち最少の試合数です。内訳には各候補の割合・相性・試合数を表示します。

## データの管理

- アーキタイプ名・デッキ名は自由に変更できます。参照済みの分類を変更する場合は別項目を作ります。
- 使用済みのデッキ・アーキタイプを削除するとアーカイブされます。過去の履歴や評価は残り、編集画面で再表示できます。
- 現在環境、または戦績・評価・分布がある環境は削除できません。
- 外部通信・アカウント・iCloud同期はありません。

### JSONバックアップ

「JSONを書き出す」で全データをファイルへ保存できます。日時はJSONEncoderのDate形式（2001年1月1日UTCからの秒数）、UUIDは文字列、勝率と割合は0〜100です。`version`で形式を識別します。

「JSONから復元する」は全件置換です。ファイルの形式・参照先・数値を検証し、置換前後の件数を確認してから復元します。置換前のデータはアプリのDocuments内「復元前バックアップ」へ自動保存します。設定画面の一覧から共有・書き出しできます。

自動バックアップもアプリ内にあるため、アプリ削除では失われます。大切なデータはファイルアプリの別の場所へ書き出してください。

## 開発構成

- `Domain.swift`：値型モデル、入力検証、集計、相性・BO1計算。
- `Store.swift`：SwiftDataの保存境界、全件検証、JSON入出力と復元。
- `App.swift`、`AnalysisViews.swift`、`SettingsViews.swift`：4タブと編集画面。
- `Intents.swift`：App Shortcut、入力セッション、対話型Snippet。
- `Tests`、`UITests`：計算・保存・復元・画面の自動テスト。

小規模な個人データを、バージョン付きの値型集合としてSwiftDataの1レコードに保存します。変更全体を1回のトランザクションで確定し、失敗時にはロールバックします。SQLの行別検索やクラウド同期向けの構成ではありません。入出力上限は50MBです。

## ビルドと実機での確認

Xcodeで`ShadowRecord`スキームを選び、BuildまたはTestを実行してください。端末へ入れる前の署名なしビルド例：

```sh
xcodebuild -project シャドバ戦績研究.xcodeproj -scheme ShadowRecord -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO build
```

実機では、Action Buttonの割当・認証、ゲーム表示中のSnippet、アプリ未起動時の記録、背面タップ、保存後の閉じ方、前回デッキの保持を確認してください。ビルド成功と、物理ボタンを使う実機確認は別です。


### シミュレータの自動テスト

`xcrun simctl list devices available`でiOS 26以降のSimulator IDを調べ、次を実行します。

```sh
python3 Tools/test_simulator.py --simulator SIMULATOR_UUID --identity 'Apple Development: 自分の証明書名'
```

既存の開発証明書を使って、テスト用アプリをローカル署名します。証明書の作成・外部送信・実機へのインストールは行いません。`--identity`を省略しても本体・計算のテストは実行できますが、この環境では未署名／アドホック署名のApp Shortcut実行をOSが拒否しました。該当する統合テストは理由付きでスキップされます。

Shortcutの統合テストはSimulatorのアプリに「検証用デッキ」と戦績を保存します。個人データを入れていないテスト用Simulatorで実行してください。日本語設定のSimulatorで実行してください。Xcodeプロジェクトは`python3 Tools/generate_project.py`で再生成できます。
