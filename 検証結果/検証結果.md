# 検証結果

検証日：2026年9月16日。Xcode 26.5（17F42）、iOS SDK 26.5。

## ビルド

- ✅ iPhone向けReleaseビルド：成功。アプリのコンパイルエラー・警告なし。
- ✅ Simulator向けDebug／テストビルド：成功。
- UIテストターゲットのみ、App Intentsメタデータ抽出を省略するツール警告があります。UIテスト自身はApp Intentを定義しないため、アプリの動作には影響しません。
- 実機向けビルドは署名なしです。実機へのインストールにはXcodeで自分の開発チームを設定してください。

## 自動テスト

✅ iOS 26.5 Simulatorで、単体13件＋UI／Shortcut統合3件＝16件すべて成功。スキップ0件。

実行ログ：[ビルド記録](ビルド記録.log)／[自動テスト記録](自動テスト記録.log)。

検証した計算・保存処理：勝率と先後内訳、サンプル区分、環境分離、主観値と実戦値の分離、分布の加重平均、未設定セル・0%重み、2デッキの最低値と同値、入力検証、JSON往復、不正JSONの拒否、SwiftData再読込、復元前バックアップ、失敗時のデータ保持、Snippetの入力遷移と重複保存防止。

画面検証：戦績の保存、4タブ、主観編集、2×2の50%対35%の比較、実戦値不足の表示、バックアップ入口、Shortcutからの記録と本体再起動後の件数増加。

## ⚡ 実際に確認したShortcutの操作

開発証明書で署名したアプリをiOS 26.5のiPhone 17 Pro Simulatorに入れ、本体プロセスを終了。ショートカットアプリに自動登録された「戦績を記録」を実行しました。

相手クラス → そのクラスのアーキタイプ → 勝敗・先後の4択 → 保存の4タップで、Interactive Snippet内の記録が完了しました。保存後はOSの完了表示を閉じます。本体を開き直した後も保存された戦績を確認しました。

この環境では未署名／アドホック署名だと、OSがApp Shortcutの実行を拒否しました。システムログにTeam IDを取得できない記録があり、既存のApple Development証明書でローカル署名すると実行できました。再現用の`Tools/test_simulator.py`を用意しています。

自動テストでは、Snippet内ボタンのアクセシビリティ座標が表示位置とずれるため、表示されたラベルの中心座標をタップしています。アプリの入力処理を直接呼ぶ代わりではなく、画面上の実操作です。

## 📱 実機での確認が必要な項目

2026年9月17日、接続したiPhone Air（iOS 26.6.2）へ開発署名付きRelease版をインストールし、起動コマンドの成功を確認しました。画面上の操作確認は未実施です。Action Button長押し、ゲームの上での表示、背面タップ、ロック時の認証、端末固有の文字サイズ・長い名称は未確認です。SimulatorでShortcutから呼び出す経路と、物理ボタン経路は分けて扱います。

Snippetの表示位置や完了ボタンはOSが管理します。保存後に任意のアプリへ強制復帰する処理はありません。[AppleのSnippet設計説明](https://developer.apple.com/videos/play/wwdc2025/281/)に沿っています。

## 画面

- [高速入力の保存確認](高速入力の保存確認.png)
- [高速入力の保存完了](高速入力の保存完了.png)
- [戦績](戦績.png)
- [主観値・実戦値の相性表](相性表.png)
- [2デッキの比較](持ち込み解析.png)
- [デッキ／設定](設定.png)

本体画面はテスト用データを使用しています。数値は実際のShadowverse対戦データではありません。

## コードの見直し

保存失敗・不正インポート・参照済み分類の変更・環境混在・未設定値の補完・二重保存を中心に自己レビューしました。保存は検証済みデータを1回のSwiftDataトランザクションで反映し、失敗時はロールバックします。参照済みデッキとアーキタイプはアーカイブし、主観未設定や実戦0戦を50%で埋めません。独立した第三者レビューは実施していません。


## 2026年9月17日：設定入口・クラスアイコンの追加

戦績画面の先頭に「入力ボタンを設定」を追加。Action Button／背面タップを選んで手順を確認し、iPhoneの設定アプリを開けます。設定タブには7クラスの絵文字編集を追加しました。主観値・戦績の識別には従来のクラスIDを使うため、アイコン変更で集計対象は変わりません。アイコンを含まない既存JSONも読み込めます。

専用設定ページへの直行は実装していません。SDK 26.5のUIKitとAccessibilityを確認し、Action Button／背面タップへ移動する公開APIは見つかりませんでした。[openSettingsURLString](https://developer.apple.com/documentation/uikit/uiapplication/opensettingsurlstring)は自分のアプリ設定を開くAPIです。その入口と、設定トップからの経路を表示します。[ShortcutsLink](https://developer.apple.com/documentation/appintents/shortcutslink)でこのアプリのApp Shortcutsページも開けます。


更新版1.1（ビルド2）：署名付きReleaseビルド成功。iPhone Airへ上書きインストール・起動成功。単体14件と既存UI／Shortcut3件が成功しました。新設定UIテスト1件は、外部設定アプリを開いた直後の戻る操作で自動操作が失敗したため、設定アプリを開く検証を最後に移し、再実行で成功しました。設定アプリへの遷移とアイコン編集画面を確認しています。未解決のアプリコンパイルエラー・警告はありません。

- [入力ボタン設定案内](入力ボタン設定案内.png)
- [クラスアイコン設定](クラスアイコン設定.png)
- [更新版ビルド記録](更新版ビルド記録.log)
- [回帰テスト記録](更新版回帰テスト記録.log)／[設定画面の再検証](設定画面再検証記録.log)

## 2026年9月17日：内蔵App Shortcutの案内とControls

バージョン1.2（ビルド3）。Action Button向けは内蔵の「戦績を記録」を選ぶだけと明記し、アクセス許可ページを開くボタンを削除しました。設定の検索欄から「アクションボタン」を探す方法も案内しています。背面タップの準備は別項目に分けました。

WidgetKit拡張で「戦績入力」Controlを追加。コントロールセンター・ロック画面・Action Buttonのコントロール候補に提供します。共有のOpenIntentをアプリと拡張の両ターゲットに含め、コントロールセンターから本体の入力画面を直接開く経路をSimulatorで確認しました。Controlは記録を自動保存しません。小さいSnippetの主経路は従来のApp Shortcutです。

Action Button／ロック画面からの実操作は別途実機確認が必要です。Control CenterからはSnippetを表示できないというApple公式の制約を踏まえ、Controlでは本体を開く方式にしています。

参考：[AppleのControl実装方法](https://developer.apple.com/documentation/widgetkit/creating-controls-to-perform-actions-across-the-system)、[内蔵App Shortcuts](https://developer.apple.com/design/human-interface-guidelines/app-shortcuts)。


✅ 最終検証：単体15件＋画面・Shortcut・Control統合5件、計20件成功。署名付きReleaseビルド成功、アプリ・拡張のコンパイル警告なし。iPhone Airへ1.2を上書きインストールし、起動成功。

[Controlからの入力画面](コントロールからの入力.png)／[設定案内](コントロール設定案内.png)／[テスト記録](コントロール版テスト記録.log)／[ビルド記録](コントロール版ビルド記録.log)


## 2026-09-17 設定移動ボタン追加（1.2.1 / 4）

- ホーム上部の入力ボタン設定に、コントロール方式の割り当て手順と「iPhoneの設定を開く」を追加。各詳細案内にも配置。
- 公開API UIApplication.openSettingsURLStringを使用。アクセス許可の変更が不要なこと、設定トップへ戻る手順を明記。失敗時は手動起動を案内。
- Release署名付きビルド成功。コンパイルエラー・警告なし。接続済みiPhoneへ更新・起動済み。
- 今回はUI自動テスト未実施。実機での設定ボタンのタップと割り当て操作は未確認。

## 2026-09-17 高速入力の起動導線統一（1.3 / 5）

- 主経路は内蔵「戦績を記録」から小さい入力画面。補助はControlから本体の高速入力画面。設定のアクセス許可画面へ送るボタンを削除。
- ホーム入口・設定案内・README・使い方を統一。クラス候補なしはアーキタイプ選択を省略。通常3〜4タップ（終了表示の操作は別）。
- SwiftDataスキーマ、保存先、既存のIntent／Control識別は維持。履歴修正用フォームは維持。
- 単体16件成功。UI5件は、初回4件成功、複合ラベルの検索指定を修正して残り1件成功。開発署名付きでControl・案内の2件を再実行し成功。署名なし再テストに付随したControl拡張のクラッシュは、署名条件を揃えた再テストの添付にはなし。
- Simulatorで内蔵操作からSnippetを表示し、4タップ保存、本体の履歴への反映、アプリ再起動後の永続化を確認。Controlから高速入力画面、戦績追加、相性表・持ち込み、案内・アイコンも確認。
- Releaseビルド成功、アプリ・拡張のコンパイル警告なし。テスト専用ターゲットのAppIntentsメタデータ抽出不要の警告のみ。
- 接続iPhoneへ上書きインストール成功。ロック解除後、07:58:15に起動成功。データ消去操作なし。
- 実機未確認：Action Button候補選択・物理長押し・ゲーム上でのSnippet表示と保存・Control Center追加と起動・ロック中の認証。背面タップの内蔵操作直接選択も未確認のため推奨しない。
- テスト用Simulatorは終了済み。iPhoneのアプリはユーザー確認用に開いた状態。

## 2026-09-17 1タップ入力・ライブ戦績（1.4 / 6）

- Xcode 26.5 / iOS SDK 26.5。最低iOS 26維持。実機用署名付きReleaseビルド成功、アプリ・拡張のコンパイル警告なし。
- 最終テスト25件成功（単体20・UI5、スキップなし）。旧JSON・旧SwiftDataのv1からv2移行、未知の相手、4結果、重複排除、取り消し、使用デッキ変更時の保存拒否、固定デッキ、集計・永続化・バックアップ・既存分析画面を検証。
- 実際の内蔵App ShortcutからSnippetの4ボタンを表示。1タップ保存後の数値変更、本体非foreground、履歴反映、再起動後の保持をSimulatorで確認。
- Live Activity開始・終了が動作。保存前後のスクリーンショットでDynamic Islandの勝利数更新を確認。コンパクト表示は勝敗のみ。
- 戦績の唯一の保存先は既存SwiftData。LiveActivityIntentは本体プロセス、WidgetはActivityContent表示のみ。App Groupへの移設なし。旧データの移行前JSONをDocuments/移行前バックアップに保存。
- iPhoneはユーザー申告により未接続。今回の実機インストール・Action Button・ゲーム上・横向き・ロック画面の実機確認は未実施。Simulator成功を実機成功とは扱わない。
- 実機用成果物: /tmp/ShadowLive/Build/Products/Release-iphoneos/ShadowRecord.app
- 作業用Simulatorは終了済み。非公開API・URL Scheme・PiP・独自overlayは未使用。

- 画面遷移完了を待つ撮影テストも成功。4ボタン・保存後・ホーム上のライブ表示を確認。極小表示で勝率が折り返したため整数%・1行へ調整し、Releaseを再ビルドして成功。極小表示調整後の実機見た目は未確認。

## 2026年9月17日：アプリ内高速入力の無反応修正（1.4.1 / 10）

- 通常アプリ内の`Button(intent:)`からシステムSnippetが表示されるという未確認の前提を削除した。
- 「⚡ 高速入力を試す」は、同じDraft・保存・集計処理を使うアプリ内プレビューを確実に表示する。Action Button用のApp ShortcutとInteractive Snippetは維持した。
- 単体21件と、先後切替・勝敗選択・保存・履歴反映の画面操作テスト1件が成功した。
- iPhoneに残っていた`ShadowRecordUITests-Runner`を削除し、本体だけが残っていることを確認した。
- 署名付きRelease Build 10をiPhone Airへ上書きインストールし、起動と端末上の版番号を確認した。既存データの削除操作は行っていない。
